An ESP32 controller that sends commands to a pet training collar via an RF OOK modulator.

This works on the PiShock board, which is a traditional ESP32 v2 board with an RF modulator attached
to one of the pins.

